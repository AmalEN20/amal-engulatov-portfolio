# Home hero motion contract

1. **Narrative purpose:** identify Amal through one semantic `Amal Engulatov` heading whose visible editorial units are `Amal` and `ENG`, optically balanced at an equal offset from the centered, waist-up ASCII portrait. The name uses a very light Geist cut with protected mask padding. A light, borderless two-sided metadata object follows `ENG` and uses only verified geography (`Seattle, Washington / New York`) and focus areas (`Frontend Development / Product · AI`). The scene resolves into the actual Home `Work` section.
2. **Entry:** `Amal` and `ENG` rise from separate clipped masks with a restrained stagger. The centered portrait keeps its approved upward clip reveal. The metadata object enters after `ENG`, sits 14px farther right on desktop (6px farther right on mobile), and begins its synchronized 12-second `Based in / Focused on` face-and-value cycle only in the resting state. The shared SiteShell remains the owner of first-load and route-transition timing.
3. **Scroll interval:** the approved trajectory contract is locked. Its entry timeline still owns exactly 420vh of desktop travel, matching the former 520svh wrapper, before a separate 1280vh Work-to-About-and-portrait sequence and an isolated 350vh About-to-Contact exit run inside the extended 2150svh wrapper. The initial identity hold is shortened from 10% to 2.5%, while the complete scatter keeps the same 50-percentage-point interval and therefore the approved physical speed and smoothness. All particle beats shift earlier by 7.5 points without changing their relative progress: the baseline curve runs from 2.5% through the 23.1% lock frame; retained upper-directed tangents continue until the Canvas clears at 52.5%. Samples with a downward tangent do not enter the exit phase, so no visible glyph can reverse or be redirected downward. At the 23.1% lock, the fully erased DOM identity becomes hidden and clipped erase trails stop rendering, preventing a late white rectangular mask. Glyph opacity does not fade; retained samples continue along their locked tangent until they cross a viewport edge. The empty beat remains 52.5–60.5%. At 60.5%, the pinned stage hands directly to the real Work card section; there is no intermediate centered `Work` title. There is no late cleanup cohort, generated glyph head, rectangle fill, binary seal, DOM opacity wash, re-entry, or follow-up particle in the Hero-to-Work timeline.
4. **Exit, reverse, and navbar signal:** every page-local transform and erase trail is derived from the same normalized progress, so reverse scroll reconstructs the exact composition and restores the text along the particle paths. Home owns `html[data-home-hero]`: `intro` at rest, `exiting` after 5%, and `cleared` at or beyond 53%, immediately after the particle Canvas clears; reverse returns to `exiting/intro`, and unmount deletes the attribute. Portfolio Main owns the matching shared navbar reveal and its calm solid-black brand treatment. `Amal` is the only Home control in the navbar; there is no duplicate `Home` link. Route exit remains owned by the shared state machine.
5. **Layer ownership:** DOM/CSS own semantic text, protected erase-target masks, the faster cardless metadata turn, the balanced identity positions, the empty beat, and the pinned Work cards. The temporary diagnostic scroll counter has been removed. The existing full-viewport decorative Canvas is portaled directly to `body` and owns the high-count glyph trajectories, white clipped erase trails, ambient drift, pointer displacement, and graphite halo. It stays below navbar at rest, rises alone above navbar while particles travel, never accepts pointer input, and returns below navbar once erasure completes. The Home passive listener reads scroll emitted by the shared SiteShell owner, writes CSS progress, and updates a ref consumed by the existing portrait RAF. It creates no Lenis instance, scroll RAF, WebGL context, video, or animation-library timeline.
6. **Variants:** desktop places `Amal` and `ENG` on opposing sides of the portrait. Narrow fine-pointer viewports retain the shared wheel/trackpad inertia instead of losing it at the mobile layout breakpoint. Mobile keeps the same 300vh entry travel inside a 1680svh wrapper, followed by 1020vh for Work exit and the three-wave About assembly plus a separate 260vh About-to-Contact exit; it uses smaller light type, native touch momentum, and capped glyph density. Reduced motion renders four ordinary 100svh sections: a static complete hero, immediately visible Work, immediately visible About without curtains or the decorative particle portrait, and an immediately usable Home Contact call-to-action; it reports `data-home-hero="cleared"` so Main can show the navbar brand without decorative delay.
7. **Budget and cleanup:** the portrait source remains about 36 KB and is used only as the hidden Canvas sampling input; the photographic `<Image>` fallback never becomes visually visible, preventing a route-mount flash of the source photograph. Canvas DPR is capped at 1.5 desktop and 1.15 touch. Desktop rendering follows each available display frame (nominally 60 fps) for the smoothest scrub, while coarse-pointer rendering is capped near 42 fps; the loop stops offscreen. Exit vectors are calculated only when the portrait is sampled and reuse the existing Canvas loop. RAF, observers, pointer/media listeners, source listener, and `data-home-hero` clean up on unmount.
8. **Acceptance:** the complete accessible name remains intact while visible text reads `Amal / ENG`; the metadata sits slightly farther right without colliding with the viewport and changes faces/values on a synchronized 12-second cycle; visible scatter begins after only a short 2.5% hold; the approved Hero particle path, duration, density, tangent lock, upper-only exit, Canvas cleanup, and reverse behavior remain unchanged relative to particle progress; the name and metadata are fully erased before their DOM visibility switches off at 23.1%; erase masks leave no late rectangular void; the Hero portrait cohort is empty at and after 52.5%, and the separate About portrait cohort cannot appear before About; 52.5–60.5% is an empty stopped frame; one semantic Work section with the two project cards starts at 60.5%, with no visible transitional title; the only Home control is the `Amal` brand, which stays hidden through identity exit and switches to `cleared` at 53%, reverses correctly, and deletes on unmount; cursor response still eases; resizing a fine-pointer browser above and below 769px keeps the same shared inertia; 390px and 430px have no horizontal overflow and retain native touch momentum; reduced motion has no inertia, pin, scatter, metadata loop, cursor loop, blur, erase travel, automatic project-image pan, or About figure assembly; no console errors; lint and build pass.

## Shared first-load, navbar, and route-surface visual refinement

1. **Purpose and legibility:** the real shared `Amal` link keeps its semantic text and internal Home destination as a calm solid-black mark at moderate weight. It uses no duplicate pseudo-copy, gradient, transparent fill, or independent animation, so Projects can continue to control the real text color through its existing difference-mode override.
2. **Home entry and reverse:** the existing `intro / exiting / cleared` threshold remains unchanged. At `cleared`, the brand becomes visible immediately and eases from its clipped upper position into place; reverse scroll applies the same opacity and transform response in reverse, disables pointer input immediately, then marks the link hidden only after the travel completes. No page-local timer or navigation state is added.
3. **Cold-load intro:** the first-load-only phase order, timers, body lock, and upward curtain exit remain unchanged. The existing curtain becomes near-black, the two white copy lines share the Projects editorial Geist scale, weight, tracking, and line-height, and the second line remains the calmer full-white emphasis. Internal navigation back to Home never replays the intro.
4. **Route cover:** the shared state machine, surface geometry, cover duration, navigation commit, body lock, focus restoration, and scroll reset remain untouched. CSS only softens the source-page dim to a 14% black backdrop over 460ms and changes the existing one-pixel leading edge to white; the backdrop remains absent during `covered` and `revealing`.
5. **Variants, budget, and acceptance:** the treatment uses only color, typography, opacity, and transform; it adds no blur, renderer, observer, or RAF. Reduced motion skips the intro, removes brand travel, keeps the brand immediately visible and clickable, and preserves instant route navigation. Acceptance requires intentional loader hold/middle/exit frames, deterministic brand forward/reverse threshold frames, one stage under repeat click, correct Home ↔ Contact/Projects navigation and Back/Forward, Projects-nav contrast, no 390/430 horizontal overflow, no console errors, and passing lint, TypeScript, and build.

## Home Work section extension

1. **Narrative purpose:** the real semantic Work section now replaces the former centered `Work` transition frame. A visible editorial heading, `What I’ve been working on lately.`, frames exactly two existing projects, Evele Studio and Amal AI Studio, using their existing imagery and verified case-study copy. No second Work section follows it.
2. **Entry hierarchy:** after the approved empty frame ends at 60.5%, the blank white Work surface appears. The editorial heading uses the Projects page's established compact Geist scale, carries a restrained looping graphite-to-silver fill while Work is visible, and reveals beneath its own background-white scroll curtain from 61.5–70.5%. Evele Studio's dedicated curtain descends from 64–80%; Amal AI Studio's follows from 72–90%. Each card's complete gray frame sits beneath its own curtain, so the line and card appear together from top to bottom with no separate frame animation, premature outline, shadow, label, count, divider, or card index. The accepted first info-panel curtain keeps its 72–90% left-to-right reveal. The lower panel uses its own left-to-right curtain from 76–90%, beginning only once enough of the second card is physically visible while retaining the same fully-open end beat. Metadata, title, summary, and CTA remain one intentional group rather than four independent child animations. Both `2026` values stay independently clipped until the cards are fully settled, then rise together with a restrained smooth reveal from 90–96.5%.
3. **Scroll interval, typographic media, and reverse:** the heading, two card curtains, card settle, two info-panel reveals, and final year reveal use named beats on the existing normalized Hero entry progress and reverse exactly with it. Evele Studio keeps its centered semantic-free DOM artwork and exact metallic wordmark treatment on `#09090a`. Amal AI Studio uses the same black field, font family, font size, tracking, line-height, seven-stop silver gradient, text glow, and looping background position; a small local variable-weight optical correction keeps its diagonal-heavy capitals from reading bolder than `EVELE`. Neither card contains or pans a screenshot. Reverse entry scroll returns both info curtains and the year masks through their exact positions; the later Work-to-About exit still lets the main card curtains cover the fully readable panels without adding a competing info exit.
4. **Layer ownership:** semantic HTML, the two local DOM wordmarks, CSS Grid, one info-panel curtain per card, and compositor-friendly transforms own the section. The visible H2 preserves one complete accessible text node. One absolute background-white DOM curtain belongs to the heading and to each card; each info panel consumes its own named scroll progress from the existing entry timeline, while both years consume one later shared progress and the real copy remains complete beneath the masks. Home's existing passive scroll listener derives these values without adding a timer, observer, animation state, or second scroll owner. The project cards use the shared `TransitionLink` API so navigation remains owned by SiteShell. Home no longer applies a second opacity dim during route cover; the shared backdrop is the only shade owner, while Home retains its local recede scale. It adds no Lenis instance, RAF loop, observer, animation library, image request, video, SVG, Canvas, or WebGL layer. Covered cards are inert and removed from the accessibility tree until their reveal is legible.
5. **Variants:** desktop cards stay approximately 160–184px high while spanning almost the full viewport width, with media on the left and copy on the right. Mobile keeps the same horizontal information architecture at approximately 160–172px high, with a narrower media column, smaller type, shorter settle distance, and native touch scrolling. The responsive height is explicit so copy cannot expand the cards. Reduced motion unpins the scenes, removes both curtains and media travel, and presents both cards immediately.
6. **Budget and cleanup:** no media request or motion dependency is introduced. Both Home artworks are CSS/DOM text. Curtain reveal uses compositor-friendly transforms; the heading, wordmark, and Amal AI surface gradients animate only background position and pause outside Work. Reduced motion removes the info-panel curtain and freezes each gradient at a static midpoint. The existing Home scroll and resize listeners remain the single cleanup owner.
7. **Acceptance:** the Hero particle choreography and its timing constants through the 60.5% handoff remain unchanged; the visible Home Work heading matches the Projects editorial font family, weight, tracking, line-height, and responsive size; its own curtain is scroll-linked; no visible `Work` label, selected-project copy, divider, or `01 / 02` index remains; the covered state is visually identical to the white page background; each complete gray card frame appears together with the card beneath its top-to-bottom curtain and has no separate draw/fade; the first info curtain retains the accepted pacing, the lower left-to-right reveal remains readable while its card opens, both finish when the second card reaches 100%, and both years reveal only after that settled frame; exact reverse entry reconstruction works without timers; Evele remains unchanged; Amal AI uses the same font size and silver-gradient poster treatment on black with a restrained optical weight correction; neither poster pans; a Home card click uses the same single shared backdrop shade as every other route transition; both cards remain keyboard accessible; desktop and mobile cards remain visibly long and low; 390px and 430px have no horizontal overflow; reduced motion presents all content immediately with static gradients and no delayed copy; project copy matches the existing case studies; no console errors; lint, TypeScript, and build pass.

## Home Work-to-About extension

1. **Narrative purpose:** close the completed project selection as one ordered scene and hand the Home narrative to its real semantic `About` section without an unanimated placeholder between them.
2. **Exit order:** a separate normalized outro begins only after the unchanged Work entry timeline completes. The heading, Evele Studio, and Amal AI Studio all use one shared 2–8.4% exit interval. Their existing white curtains return from below at exactly the same scroll progress, so the heading, both card contents, and both complete frames disappear together at the same physical pace as the approved shorter outro. About cannot begin revealing until the synchronized covers have completed.
3. **About reveal and three assembly beats:** after Work is fully covered, About's white surface clips upward from 9.6–13% of the outro. Each accepted text group and its particle flight form one uninterrupted but strictly ordered gesture: row one reveals from 14.2–28.2%, then wave one flies from 28.2–41.2%; row two reveals from 41.2–55.2%, then wave two flies from 55.2–68.2%; row three reveals from 68.2–82.2%, then wave three flies from 82.2–95.2%. The longer wrapper makes every text curtain roughly twice as slow in physical scroll distance, while each accepted particle flight retains its prior physical distance and speed by occupying 13 normalized points of the enlarged scene. A wave begins on the exact frame its text curtain finishes, and the next text cannot start until the preceding wave has resolved. Every wave originates only at the right endpoint of a short emitter line centered against the complete heading-and-copy group. Wave input stays linear before each particle applies its accepted easing and stagger, avoiding a hidden double-ease pause at launch. Each cohort remains a deterministic scattered third of the full portrait, while the final targets return to the exact unjittered Hero sample geometry so the completed silhouette matches Hero instead of appearing warped. Every value is derived from scroll progress; stopping preserves an intentional scattered frame, and reverse scroll follows the same order in reverse.
4. **Layer ownership and accessibility:** Home DOM/CSS owns the returning Work curtains, About surface clip, the three horizontal row curtains, and one restrained graphite-gradient emitter line per paragraph. Each line draws completely from its left edge before its precise circular endpoint marker begins to appear; the point therefore never precedes the finished line. The line stays present through the accepted maximum launch stagger, then retracts left-to-right from wave progress 28–42% and is fully hidden while the emitted cloud is still travelling and assembling. It does not wait for particles to reach the portrait; reduced motion hides it. The existing `AsciiPortrait` Canvas and its existing RAF own the three accepted radial scatter clouds, the delayed circular source marker per active wave, and the enlarged bottom-anchored Hero portrait target, reusing the same hidden sampling asset and glyph alphabet without a new visible photograph, renderer, RAF, observer, dependency, or WebGL context. On each real About-scene activation, including a Home mount beneath the shared route surface, Canvas refreshes the current emitter and erase-target DOM rectangles once before drawing; mount-time font/layout coordinates are never reused after the destination settles. This geometry refresh does not alter seeds, paths, stagger, density, or flight math. Particles remain invisible while they overlap the marker, become visible after leaving its radius, and grow from their sub-pixel launch floor. On fine pointers, the completed About portrait reuses Hero's eased local particle displacement and settles back when the pointer leaves; coarse pointers and reduced motion omit it. During About's exit only, the Canvas rises to layer 99 while the navbar remains at 100 and its white blur surface becomes transparent; glyphs therefore remain visible travelling behind the real `Amal` brand and nav links until they cross the viewport edge. Assembly and resting frames retain their lower layer, the Canvas clears when the Home scene leaves the viewport, and it never accepts pointer input. One semantic About H2 labels the section and each row has its own H3; the emitter lines and source markers are decorative and hidden from accessibility APIs.
5. **Variants, budget, and acceptance:** desktop receives 1280vh of post-entry travel, keeps the compact text panel on the left, centers the roughly 92vh portrait optically within the remaining space between copy and the right viewport edge, and anchors it to the lower edge. Mobile receives 1020vh, narrows the text panel and emitter lines, reduces glyph density, and moves the roughly 62vh portrait slightly inward without horizontal overflow or touch hijacking. The longer scene slows the text curtains while preserving the accepted physical particle-flight speed across all three waves. Canvas DPR remains capped at 1.5 desktop and 1.15 coarse pointer, and the one existing frame loop remains the only particle loop. Reduced motion removes the About curtains, emitter lines, decorative portrait, and pointer response entirely while presenting all copy immediately. Acceptance requires: every text to finish before its line and particles appear; line and wave to start without a blank scroll gap; every line to begin retracting immediately after its final particle launches and disappear well before that cloud lands; particles to leave only from the line endpoint; three accepted irregular clouds without line-like travel; three visually distinct whole-portrait partial states; a recognizable unwarped Hero-matching final portrait centered in the available right-side space; exact reverse reconstruction; local pointer displacement and smooth settle on desktop; no glyph leakage into Work or Contact; no divider lines; `#about` to target the real Home About section; zero overflow at 390/430; and clean console, lint, TypeScript, and build.

## Home About-to-Contact extension

1. **Narrative purpose and invariant:** keep the Home About scene identified through the stronger `Open to / Curiosity / Approach` band labels without adding a separate visible `About` heading, then turn the completed portrait into the transition device for the real Home Contact call-to-action. The semantic H2 remains available to assistive technology, and the accepted three-band copy remains unchanged and readable until the exit begins.
2. **Entry and resting motion:** the accepted three row curtains and three assembly flights retain their exact physical distances and paths. Once individual glyphs settle into the completed portrait, they inherit Hero's restrained continuous row/column drift and glyph flicker while preserving the existing local pointer displacement. This ambient motion uses the existing Canvas RAF and stops entirely for coarse-pointer reduced-motion rendering.
3. **Exit interval and stopped frames:** after the 1280vh assembly sequence completes, a separate normalized exit owns 350vh desktop and 260vh mobile. At the start frame the complete moving portrait and all About copy are intact. Through the middle, deterministic particle cohorts travel on varied straight vectors toward upper-left, middle-left, and lower-left destinations; no particle receives a rightward target or curved exit arc. White trails are clipped only to the visible labels, headings, and paragraphs so the flight appears to erase the copy. Every particle reaches a destination beyond the left viewport edge by 76% of this interval, after which the Canvas clears. At the end the About text is unavailable and Contact is the only readable scene. Reverse scroll reconstructs the same paths, copy, and portrait exactly.
4. **Contact reveal and ownership:** the Home Contact surface lives in the same sticky Home stage and reveals only after the leftward particle wipe and Canvas have cleared. The white surface remains scroll-linked from 76–82%, and the semantic heading `If you wanna talk.` retains its accepted word-by-word clipped rise from 82–89%. When the final heading word reaches its complete frame, one dataset threshold starts a non-scrubbed CSS sequence: the centered vertical hairline draws downward for 900ms, and the smaller `Let’s talk` link begins its 680ms masked rise after 640ms. Continuing or stopping scroll does not scrub either autonomous response. The heading keeps the Projects page's compact `I like making new things.` Geist scale; the action uses the same family, weight, tracking, and line-height at a smaller size. The redundant visible `Contact` label, underline, and arrow remain removed; the action's restrained graphite-to-silver gradient loops only while the sequence is visible and keeps a visible focus outline. When reverse scroll moves the completed heading back below its hysteresis threshold, the action descends and fades first, then the hairline retracts toward its top edge with the same autonomous timing hierarchy in reverse; a new full reveal replays the entrance. The CTA uses `TransitionLink` with label `Contact`, so the shared SiteShell remains the only route-transition owner. This surface does not duplicate or modify the Contact form contract. Home's existing passive scroll listener derives the surface/title progress and one autonomous threshold. The existing `AsciiPortrait` Canvas owns particle positions and clipped erase trails; there is no new Lenis instance, RAF, observer, animation library, SVG, video, or WebGL layer.
5. **Variants, reduced motion, budget, and acceptance:** mobile shortens the exit and reduces simultaneous glyph density while keeping native touch scroll. Reduced motion presents About and Contact as normal consecutive sections with no ambient portrait movement, particle travel, wipe, clip, or delayed link. Canvas DPR, frame-rate caps, cleanup, and the single-RAF budget remain unchanged. Acceptance requires: no visible standalone `About` heading; stronger but restrained band labels; every wave to launch from a vertically compact 2.4px source at the line endpoint; glyphs to begin near 8% of their final size with a 1px floor and grow smoothly as the deliberately narrow early fan opens into the accepted scattered flight; each emitter line to open left-to-right and retract left-to-right; constant subtle portrait motion after assembly; all exit destinations left of the viewport with visibly varied upper/middle/lower angles and no arc; a restrained deterministic subset of otherwise straight leftward trajectories to lead the main cloud slightly and cross every text erase rectangle so `Approach` and its `…roach` fragment are physically covered in the middle exit frame; the late clipped cleanup pass remains a safety frame; Contact does not become legible prematurely; exact reverse restoration; keyboard access and visible focus on the Contact link; zero horizontal overflow at 390/430; and clean console, lint, TypeScript, and build.
